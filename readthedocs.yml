# Required
version: 2

build:
  image: latest

# Build all formats
formats: all

python:
  version: 3.7
  install:
    - method: pip
      path: .
    - requirements: docs/requirements.txt
    - method: setuptools
      path: .
